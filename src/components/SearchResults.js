import React from 'react'
import _ from 'lodash'

import AlbumsList from './AlbumsList'

const SearchResults = ({result, setCategory, selectedCategory}) => {
    const {albums, artists} = result;
    console.log(result)
    return (
        <div></div>
    )
}

export default SearchResults;